{
  "name": "@coderifts/agent-guard",
  "version": "17.0.0",
  "description": "Fail-closed guard for AI agent tool calls \u2014 preflight contract changes before they execute. Security core frozen (agent-guard-api v1.0); v1.1 adds client-side enforcement: receipt\u2192envelope binding, decision\u2194action reconciliation, safe_for_agent + degraded fail-closed, and enforced\u27faexecuted (P0 client-enforcement pack).",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/cjs/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/esm/index.d.ts",
        "default": "./dist/esm/index.js"
      },
      "require": {
        "types": "./dist/cjs/index.d.ts",
        "default": "./dist/cjs/index.js"
      }
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "build:esm": "tsc -p tsconfig.esm.json",
    "build:markers": "node scripts/write-dist-type-markers.mjs",
    "build": "npm run clean && npm run build:cjs && npm run build:esm && npm run build:vendor && npm run build:markers",
    "lint": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "test": "node --test test/*.test.js",
    "prepublishOnly": "node scripts/assert-changelog-version.js && bash \"${CODERIFTS_APP_ROOT:-$HOME/coderifts-app}/scripts/freeze-gate.sh\" && npm run build",
    "prepack": "node scripts/publish-package-json.mjs strip",
    "postpack": "node scripts/publish-package-json.mjs restore",
    "build:vendor": "node scripts/copy-vendor.mjs"
  },
  "keywords": [
    "coderifts",
    "agent",
    "guard",
    "governance",
    "ai-agent",
    "tool-safety",
    "fail-closed",
    "preflight"
  ],
  "author": "CodeRifts <hello@coderifts.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/coderifts/agent-guard.git"
  },
  "homepage": "https://coderifts.com",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@coderifts/sdk": "^3.10.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "peerDependencies": {
    "@coderifts/sdk": "^3.10.0"
  }
}
